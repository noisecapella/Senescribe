<% print "URL_FOR" + url_for(:action => :create, :controller => :user).to_s %>

<%= form_tag({:action => :create, :controller => :user}) do %>OpenID URL: 
<%= text_field_tag :openid_url %>
<%= submit_tag "Log in" %>
<% end %>
